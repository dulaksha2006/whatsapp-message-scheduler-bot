version: '3.8'
services:
  wa-scheduler:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./.wwebjs_auth:/app/.wwebjs_auth
    restart: unless-stopped
    environment:
      - PORT=3000
      - NODE_ENV=production
